# Serves the built SPA. /api is routed to the backend by the cluster Ingress,
# not here — this server only serves static assets with an index.html fallback.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
