#!/usr/bin/with-contenv bashio
export CAMERA_ROOT="$(bashio::config 'camera_root')"
export DATA_DIR="/data"
cd /app/backend
exec /opt/venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000 --log-level warning
