name: Camera Archive Cleaner
description: Browse, review, and clean camera archive files from a network share.
version: "1.1.5"
slug: camera_archive_cleaner
arch:
  - amd64
  - aarch64
ingress: true
ingress_port: 8099
panel_icon: mdi:cctv
map:
  - media:rw
options:
  camera_root: /media
  compute_remote_url: ""
schema:
  camera_root: str
  compute_remote_url: str
image: ghcr.io/sergeyanokhin/camera-archive-cleaner-addon
startup: application
